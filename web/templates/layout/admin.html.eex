<!DOCTYPE html>
<html>
<head>
  <!-- Standard Meta -->
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">

  <!-- Site Properities -->
  <title>Admin > stdout.in Ievgen Kuzminov IT blog</title>

  <link rel="shortcut icon" href="favicon.ico" type="image/png">
  <link rel="stylesheet" href="<%= static_path(@conn, "/css/vendor.css") %>">
  <link rel="stylesheet" href="<%= static_path(@conn, "/css/admin.css") %>">
  <link rel="stylesheet" href="<%= static_path(@conn, "/css/components.css") %>">

  <%= JS.print_global_js_variables(@conn) %> 
</head>
<body>

  <div class="ui menu">
    <div class="ui container">
      <a href="#" class="header item">
        Admin: StdOut.In
      </a>
      <a href="#" class="item">Posts</a>
      <a href="#" class="item">Pages</a>
      <%= button "Log out", to: main_path(@conn, :logout),
          method: :delete, form: [class: "item right aligned"] %>

      </div>
  </div>

  <div id="admin-app" class="ui main container">
    <%= render @view_module, @view_template, assigns %>
  </div>

  <script src="<%= static_path(@conn, "/js/vendor.js") %>"></script>
  <script src="<%= static_path(@conn, "/js/admin.js") %>"></script>
</body>

</html>
